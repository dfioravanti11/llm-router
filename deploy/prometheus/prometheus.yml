# Scrapes the router. Add one job per mock worker when their metrics land.
global:
  scrape_interval: 1s
  evaluation_interval: 15s

scrape_configs:
  - job_name: warmpath
    metrics_path: /metrics
    static_configs:
      - targets: ["host.docker.internal:8080"]
